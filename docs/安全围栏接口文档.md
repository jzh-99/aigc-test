# AI安全检测服务 - 接口文档

## 概述

**服务地址**: `http://YOUR_PROXY_SERVER_IP:5000`

> 注意：请将 `YOUR_PROXY_SERVER_IP` 替换为实际的代理服务器IP地址（61.155.227.138）

## 快速开始

### 前置条件

- 确保能访问代理服务器的网络
- 安装 Python 3.6+ （如果使用Python客户端）

### 30秒快速测试

```bash
# 1. 健康检查
curl http://YOUR_PROXY_SERVER_IP:5000/health

# 2. 文本检测
curl -X POST http://177.11.220.35:80/api/aigc/security/check/text \
  -H "Content-Type: application/json" \
  -d '{
    "tenantId": -173,
    "robotCode": "aiguangct2p",
    "robotType": "jaingsuhaobai-telecom",
    "content": "这是一段测试文本",
    "contentType": "userQuery"
  }'
```

---

## 接口列表

| 接口路径                           | 方法 | 功能         | 说明                         |
| ---------------------------------- | ---- | ------------ | ---------------------------- |
| `/health`                          | GET  | 健康检查     | 检查服务是否正常运行         |
| `/api/aigc/security/check/text`    | POST | 文本安全检测 | 检测文本内容是否包含敏感信息 |
| `/api/aigc/security/check/picture` | POST | 图片安全检测 | 检测图片内容是否包含敏感信息 |

---

## 1. 健康检查

### 接口信息

- **路径**: `/health`
- **方法**: `GET`
- **说明**: 检查代理服务是否正常运行

### 请求示例

```bash
curl http://YOUR_PROXY_SERVER_IP:5000/health
```

### 响应示例

```json
{
  "status": "ok",
  "service": "AI Security Check Proxy",
  "timestamp": 1706345678
}
```

### 响应字段说明

| 字段      | 类型    | 说明                    |
| --------- | ------- | ----------------------- |
| status    | string  | 服务状态，"ok" 表示正常 |
| service   | string  | 服务名称                |
| timestamp | integer | 当前时间戳（秒）        |

---

## 2. 文本安全检测

### 接口信息

- **路径**: `/api/aigc/security/check/text`
- **方法**: `POST`
- **Content-Type**: `application/json`
- **说明**: 检测文本内容是否包含敏感、违规信息

### 请求参数

| 参数名      | 类型    | 必填 | 说明                          | 示例值                         |
| ----------- | ------- | ---- | ----------------------------- | ------------------------------ |
| tenantId    | integer | 是   | 租户ID                        | -173                           |
| robotCode   | string  | 是   | 应用代码                      | "aiguangct2p"                  |
| robotType   | string  | 是   | 应用类型                      | "jaingsuhaobai-telecom"        |
| content     | string  | 是   | 待检测的文本内容（UTF-8编码） | "这是需要检测的文本"           |
| contentType | string  | 是   | 内容来源类型                  | "userQuery" 或 "modelResponse" |

**contentType 取值说明**:

- `userQuery`: 用户输入的内容
- `modelResponse`: AI模型生成的内容

### 请求示例

#### cURL

```bash
curl -X POST http://YOUR_PROXY_SERVER_IP:5000/api/aigc/security/check/text \
  -H "Content-Type: application/json" \
  -d '{
    "tenantId": -173,
    "robotCode": "aiguangct2p",
    "robotType": "jaingsuhaobai-telecom",
    "content": "这里是需要检测的敏感测试文本",
    "contentType": "userQuery"
  }'
```

#### Python

```python
import requests
import json

url = "http://YOUR_PROXY_SERVER_IP:5000/api/aigc/security/check/text"

payload = {
    "tenantId": -173,
    "robotCode": "aiguangct2p",
    "robotType": "jaingsuhaobai-telecom",
    "content": "这里是需要检测的敏感测试文本",
    "contentType": "userQuery"
}

response = requests.post(url, json=payload, timeout=30)
result = response.json()

print(json.dumps(result, ensure_ascii=False, indent=2))
```

#### JavaScript (Node.js)

```javascript
const axios = require('axios');

const url = 'http://YOUR_PROXY_SERVER_IP:5000/api/aigc/security/check/text';

const payload = {
  tenantId: -173,
  robotCode: 'aiguangct2p',
  robotType: 'jaingsuhaobai-telecom',
  content: '这里是需要检测的敏感测试文本',
  contentType: 'userQuery'
};

axios.post(url, payload)
  .then(response => {
    console.log(JSON.stringify(response.data, null, 2));
  })
  .catch(error => {
    console.error('Error:', error.message);
  });
```

#### Java

```java
import java.net.http.*;
import java.net.URI;
import com.google.gson.Gson;
import com.google.gson.JsonObject;

public class TextSecurityCheck {
    public static void main(String[] args) throws Exception {
        String url = "http://YOUR_PROXY_SERVER_IP:5000/api/aigc/security/check/text";

        JsonObject payload = new JsonObject();
        payload.addProperty("tenantId", -173);
        payload.addProperty("robotCode", "aiguangct2p");
        payload.addProperty("robotType", "jaingsuhaobai-telecom");
        payload.addProperty("content", "这里是需要检测的敏感测试文本");
        payload.addProperty("contentType", "userQuery");

        HttpClient client = HttpClient.newHttpClient();
        HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create(url))
            .header("Content-Type", "application/json")
            .POST(HttpRequest.BodyPublishers.ofString(new Gson().toJson(payload)))
            .build();

        HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
        System.out.println(response.body());
    }
}
```

### 响应示例

#### 检测通过（无敏感内容）

```json
{
  "code": "10000",
  "message": "成功",
  "data": {
    "securityCheckStatus": true,
    "commonAnomalyTags": []
  }
}
```

#### 检测未通过（包含敏感内容）

```json
{
  "code": "10000",
  "message": "成功",
  "data": {
    "securityCheckStatus": false,
    "commonAnomalyTags": [
      {
        "firstLevelTagName": "违法违规",
        "secondLevelTagName": "政治敏感",
        "thirdLevelTagName": "涉政人物"
      },
      {
        "firstLevelTagName": "社会安全",
        "secondLevelTagName": "暴力违法",
        "thirdLevelTagName": "暴力犯罪"
      }
    ]
  }
}
```

### 响应字段说明

| 字段路径                                    | 类型    | 说明                                          |
| ------------------------------------------- | ------- | --------------------------------------------- |
| code                                        | string  | 响应码，"10000" 表示成功                      |
| message                                     | string  | 响应消息                                      |
| data                                        | object  | 检测结果数据                                  |
| data.securityCheckStatus                    | boolean | 安全检测状态，`true` = 通过，`false` = 未通过 |
| data.commonAnomalyTags                      | array   | 命中的异常标签列表                            |
| data.commonAnomalyTags[].firstLevelTagName  | string  | 一级标签名称                                  |
| data.commonAnomalyTags[].secondLevelTagName | string  | 二级标签名称                                  |
| data.commonAnomalyTags[].thirdLevelTagName  | string  | 三级标签名称                                  |

---

## 3. 图片安全检测

### 接口信息

- **路径**: `/api/aigc/security/check/picture`
- **方法**: `POST`
- **Content-Type**: `application/json`
- **说明**: 检测图片内容是否包含敏感、违规信息

### 请求参数

| 参数名      | 类型    | 必填 | 说明                   | 示例值                         |
| ----------- | ------- | ---- | ---------------------- | ------------------------------ |
| tenantId    | integer | 是   | 租户ID                 | -173                           |
| robotCode   | string  | 是   | 应用代码               | "aiguangct2p"                  |
| robotType   | string  | 是   | 应用类型               | "jaingsuhaobai-telecom"        |
| content     | string  | 是   | 图片的Base64编码字符串 | "iVBORw0KGgoAAAANS..."         |
| contentType | string  | 是   | 内容来源类型           | "userQuery" 或 "modelResponse" |

> **注意**: content 字段需要传入图片文件的 Base64 编码字符串（不包含 `data:image/png;base64,` 前缀）

### 图片转Base64示例

#### Python

```python
import base64

def image_to_base64(image_path):
    """将图片文件转换为Base64字符串"""
    with open(image_path, "rb") as image_file:
        return base64.b64encode(image_file.read()).decode('utf-8')

# 使用示例
base64_str = image_to_base64('/path/to/image.jpg')
print(base64_str)
```

#### JavaScript (Node.js)

```javascript
const fs = require('fs');

function imageToBase64(imagePath) {
  const imageBuffer = fs.readFileSync(imagePath);
  return imageBuffer.toString('base64');
}

// 使用示例
const base64Str = imageToBase64('/path/to/image.jpg');
console.log(base64Str);
```

#### Bash

```bash
base64 -w 0 /path/to/image.jpg
```

### 请求示例

#### cURL

```bash
# 先将图片转为Base64
IMAGE_BASE64=$(base64 -w 0 /path/to/image.jpg)

# 发送请求
curl -X POST http://YOUR_PROXY_SERVER_IP:5000/api/aigc/security/check/picture \
  -H "Content-Type: application/json" \
  -d "{
    \"tenantId\": -173,
    \"robotCode\": \"aiguangct2p\",
    \"robotType\": \"jaingsuhaobai-telecom\",
    \"content\": \"$IMAGE_BASE64\",
    \"contentType\": \"userQuery\"
  }"
```

#### Python

```python
import requests
import base64
import json

def check_image_security(image_path):
    """检测图片安全性"""
    # 读取图片并转换为Base64
    with open(image_path, "rb") as image_file:
        base64_str = base64.b64encode(image_file.read()).decode('utf-8')

    # 构造请求
    url = "http://YOUR_PROXY_SERVER_IP:5000/api/aigc/security/check/picture"
    payload = {
        "tenantId": -173,
        "robotCode": "aiguangct2p",
        "robotType": "jaingsuhaobai-telecom",
        "content": base64_str,
        "contentUrl": "https://example.png"
        "contentType": "userQuery"
    }

    # 发送请求
    response = requests.post(url, json=payload, timeout=30)
    result = response.json()

    return result

# 使用示例
result = check_image_security('/path/to/image.jpg')
print(json.dumps(result, ensure_ascii=False, indent=2))
```

#### JavaScript (Node.js)

```javascript
const axios = require('axios');
const fs = require('fs');

async function checkImageSecurity(imagePath) {
  // 读取图片并转换为Base64
  const imageBuffer = fs.readFileSync(imagePath);
  const base64Str = imageBuffer.toString('base64');

  // 构造请求
  const url = 'http://YOUR_PROXY_SERVER_IP:5000/api/aigc/security/check/picture';
  const payload = {
    tenantId: -173,
    robotCode: 'aiguangct2p',
    robotType: 'jaingsuhaobai-telecom',
    content: base64Str,
    contentType: 'userQuery'
  };

  try {
    const response = await axios.post(url, payload);
    console.log(JSON.stringify(response.data, null, 2));
    return response.data;
  } catch (error) {
    console.error('Error:', error.message);
  }
}

// 使用示例
checkImageSecurity('/path/to/image.jpg');
```

### 响应示例

#### 检测通过（无敏感内容）

```json
{
  "code": "10000",
  "message": "成功",
  "data": {
    "securityCheckStatus": true,
    "commonAnomalyTags": []
  }
}
```

#### 检测未通过（包含敏感内容）

```json
{
  "code": "10000",
  "message": "成功",
  "data": {
    "securityCheckStatus": false,
    "commonAnomalyTags": [
      {
        "firstLevelTagName": "色情低俗",
        "secondLevelTagName": "色情内容",
        "thirdLevelTagName": "色情图片"
      }
    ]
  }
}
```

### 响应字段说明

响应字段与文本检测接口相同，请参考上文说明。

---

## 状态码说明

| HTTP状态码 | code  | 说明                           |
| ---------- | ----- | ------------------------------ |
| 200        | 10000 | 请求成功                       |
| 400        | 40000 | 请求参数错误（缺少必填字段等） |
| 500        | 50000 | 服务器内部错误或转发失败       |

---

## 常见异常标签分类

检测未通过时，`commonAnomalyTags` 字段会返回命中的异常标签。常见标签类别包括：

### 一级标签

| 标签名称 | 说明               |
| -------- | ------------------ |
| 违法违规 | 包含违法违规内容   |
| 社会安全 | 涉及社会安全问题   |
| 色情低俗 | 包含色情低俗内容   |
| 辱骂谩骂 | 包含辱骂攻击性语言 |
| 暴力血腥 | 包含暴力血腥内容   |
| 其他不良 | 其他不良信息       |

### 示例标签层级

```
违法违规
  └─ 政治敏感
      └─ 涉政人物
      └─ 敏感事件

色情低俗
  └─ 色情内容
      └─ 色情图片
      └─ 色情文本

社会安全
  └─ 暴力违法
      └─ 暴力犯罪
      └─ 恐怖主义
```

---

