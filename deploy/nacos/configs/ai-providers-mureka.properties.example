# ============================================================
# Mureka（音乐生成 / 音色克隆）
# Data ID: ai-providers-mureka.properties   Group: AI_GROUP
#
# 本文件是留空模板（.example）。复制为 ai-providers-mureka.properties 后填真实值。
# 注意：*.properties 被 .gitignore 忽略（防止密钥入库），只有 .example 进 git。
# ============================================================

# Mureka 服务基址（由服务提供方给出）
MUREKA_API_URL=

# Mureka API Key（必填）
MUREKA_API_KEY=

# 注：音色克隆扣减积分（music_voice_clone）不在此配置，属积分计费体系，
# 权威来源是数据库 system_cost_configs（key=music_voice_clone），
# MUSIC_VOICE_CLONE_CREDITS 环境变量仅作 DB 缺失时的兜底默认值，不放 Nacos。
